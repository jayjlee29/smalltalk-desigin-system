간격이 일정한 플렉스 스택. 세로/가로 정렬 배치의 기본 도구입니다.

```jsx
<Stack gap={16}>...세로...</Stack>
<Stack direction="horizontal" gap={8} align="center">...가로...</Stack>
```

`direction`(vertical/horizontal), `gap`(px), `align`/`justify`(flex 정렬), `wrap`(줄바꿈)로 조절합니다.
