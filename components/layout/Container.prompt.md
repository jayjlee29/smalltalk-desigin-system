중앙 정렬 콘텐츠 열. 앱의 단일 본문 컬럼을 구성합니다.

```jsx
<Container size="content">...본문...</Container>
<Container size="form">...로그인 폼...</Container>
```

`size`: `content`(768px, max-w-3xl — 본문), `form`(384px, max-w-sm — 인증 폼), `full`(100%). `padding`으로 좌우 여백을 조절합니다.
