작은 상태 라벨. 게시판 활성/비활성 상태, 계좌 유형 칩(ISA/연금저축/IRP/일반), 글 해시태그에 사용됩니다.

```jsx
<Badge tone="blue">활성</Badge>
<Badge tone="purple">연금저축</Badge>
```

`pill`(기본값 true)은 완전히 둥근 알약 모양을 줍니다(상태/태그용). `false`로 설정하면 작은 사각형 태그 칩 변형이 됩니다.
