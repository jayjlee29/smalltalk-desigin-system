인라인 알림 배너. 폼 상단 안내나 오류 메시지 등에 사용합니다.

```jsx
<Alert tone="error" title="오류">비밀번호가 일치하지 않습니다.</Alert>
```

톤(tone): `info`(파랑), `success`(초록), `warning`(주황), `error`(빨강). `onClose`를 주면 우측에 닫기 버튼이 표시됩니다.
