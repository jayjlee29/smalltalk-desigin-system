사용자 아바타. 이미지가 있으면 원형 이미지, 없으면 이름 첫 글자 이니셜 + 색상 배경.

```jsx
<Avatar name="스몰토커" />
<Avatar name="김재구" src="/me.png" size={48} />
```

`size`(px)로 크기를 조절합니다. 이니셜 배경색은 이름으로 결정됩니다(blue/green/purple/orange/red).
