Numbered pagination row, used identically in board list and board article list.

```jsx
<Pagination page={2} totalPages={5} onChange={setPageNo} />
```

Active page is solid `blue-600`; inactive pages are bordered white buttons that get a `gray-50` hover fill. 36×36px square buttons, `rounded-lg`.
